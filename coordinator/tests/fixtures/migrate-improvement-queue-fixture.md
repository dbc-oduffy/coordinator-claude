## Improvement Queue

_Synthetic fixture for migrate-improvement-queue-universals.py tests. Contains a mix of universal-with-central-target, universal-without-target, and project-specific entries._

- 2026-05-30 | claude-central | state/lessons.md:12 | [universal] `date -r FILE` is GNU-only — BSD/macOS reads arg as epoch seconds; the `|| echo 0` fallback silently zeroes mtime and bypasses every threshold check. Fix: use portable stat helper or `python -c "import os,sys; print(int(os.path.getmtime(sys.argv[1])))"`. | proposed target: `~/.claude/docs/wiki/cross-platform-shell-portability.md` § Portable mtime pattern (new row)
- 2026-06-01 | project-rag | state/lessons.md:7 | Embedding cache flush on model-version bump should be gated by a version sentinel in the db schema, not a manual vacuum step. This is specific to the project-rag daemon restart cycle. No cross-repo generalisation applicable.
- 2026-06-05 | claude-central | state/lessons.md:~150 | [universal] Premise re-verification at execute-plan time — concurrent EM sessions can ship overlapping work between plan-draft and execute; reconciliation step needs to fire in /execute-plan Phase 1.5, not just /pickup Step 3.4. | proposed target: `coordinator/docs/wiki/dispatching-parallel-agents.md` § Premise-pass (new section)
- 2026-06-08 | DelphiOS | state/lessons.md:14 | [universal] Stale handoff framing is hypothesis, not ground truth — always re-verify cited code before acting on symptom claims. No specific wiki path identified yet; needs triage to pick the right doctrine surface.
- 2026-06-09 | DelphiOS | state/lessons.md:19 | Atlas refresh gate: when the atlas is stale and the gate is disabled by flag, the EM silently skips freshness checks — the flag should require an explicit reason string so the override is auditable in git log. Project-specific to the atlas-gate mechanism.
- 2026-06-10 | project-rag | state/lessons.md:3 | [universal] `date -r FILE` is GNU-only on BSD/macOS — same portable mtime issue as the 2026-05-30 entry above, observed again in project-rag build scripts. Fix is identical: use portable stat helper. | proposed target: `~/.claude/docs/wiki/cross-platform-shell-portability.md` § Portable mtime pattern (confirms existing entry from different source repo)
- 2026-06-11 | claude-central | state/lessons.md:~155 | [universal] Bare interpreter name through PATH on macOS resolves to system version, not the running one. Bash subprocess invocations via bare `bash` drop to /bin/bash 3.2 even when parent is bash 5. No target wiki identified for this entry yet — needs manual review to pick correct page.
- 2026-06-12 | geneva-mvp | state/lessons.md:5 | GameLift Streams session token expiry is 15 minutes — the frontend must refresh the stream URL before the token window closes, not just at initial load. Geneva-specific, not a cross-project pattern.
- 2026-06-13 | claude-central | state/lessons.md:~8 | [universal] `git commit -m … -- <new>` after `git mv` ships only the add half — pass both old+new paths in pathspec, OR drop -- restriction with staging hygiene. | proposed target: `coordinator/docs/wiki/scoped-safety-commits.md` § rename mechanics (new row)
