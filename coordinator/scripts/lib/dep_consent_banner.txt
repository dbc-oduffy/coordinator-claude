================================================================
WARNING — UNSAFE INSTALL REQUESTED

You are installing coordinator-claude WITHOUT one or more of its
dependency-chain prerequisites:

  <!-- DEPS_LIST_PLACEHOLDER -->

Without these, your install will run without a required dependency
that this contract version of coordinator-claude needs.

To proceed anyway, you must confirm twice. The first prompt asks
whether you accept the missing-dep risk; the second asks whether
you specifically accept that running without required deps may
produce contract-violation outcomes.

[if you were a non-root chain step, this consent would be hard-blocking]
[for coordinator-claude (DAG root) this confirmation path is structurally
 dead code — direct_deps: [] means no hard deps can be missing. These flags
 exist for schema-conformance with the chain-step contract; the standard
 coordinator install path requires neither flag.]

coordinator-claude is DAG root — no upstream dependencies to install.
Override flags exist for schema-conformance only.
================================================================
