<!-- canonical source for subagent-sandbox-preamble — edit here, then run verify-subagent-sandbox-preamble-sync.py --fix -->
<!-- consumers: discovered at runtime by bin/verify-subagent-sandbox-preamble-sync.py via its _CONSUMER_ROLE_MAP (scoped Sonnet/Haiku single-job workers — scouts, specialists, workers, checkers, auditors). This body is NOT one blanket scratch offer: each consumer's report_type_map: template type (subagent-sandbox-policy.yaml) selects exactly one VARIANT block below, naming that role's own provisioned typed doc, not a generic dump target. Four exclusions, each for a DIFFERENT reason — do not read them as one category: (1) the six Opus reviewer personas get their own richer `contract_blocks:`-injected family instead (sidecar-frontmatter-contract + persona-persisting-findings), not this generic preamble; (2) executor/review-integrator likewise get their own `contract_blocks:`-injected family (run-report-citizenship) — their `run-report` artifact shape (status/agent_type/divergence/commits) doesn't match either VARIANT below, and their own agent files already carry far richer bespoke sidecar doctrine than this generic preamble would add; (3) docs-checker is a G2 pre-flight emitter — it gets sidecar-emission-contract via `contract_blocks:` instead, which is why it is absent from this file's consumer list even though it IS `contract_blocks:`-eligible (the two mechanisms are not mutually exclusive in general — prior-art-checker/plan-coverage-checker/external-pattern-checker carry BOTH this resident preamble AND sidecar-emission-contract — docs-checker is simply the one G2 emitter that never picked up this resident paste); (4) enricher has no equivalent citizenship of any kind yet — a real gap, not a reasoned exclusion, left for a future pass. A discovery comment that contradicts the policy file (subagent-sandbox-policy.yaml) is how the 2026-07-26 G1-second-pass regression drifted in the first place — keep this list honest as citizenship changes. -->

<!-- VARIANT:review-findings -->
**Your provisioned home for this dispatch: `state/subagent-share/<session-id>/<provision_key>.md` — git-tracked, review-findings-typed (one disposition slot per finding), created for your role before you start. Record each finding's disposition there as you go, then return only a terse pointer — `done: <path>`, never a full dump. Your final message spends the EM's context window; the sidecar doesn't. Fall back to `scratch/subagent-sandbox/` (root-level, off `state/`) only if your dispatch carries no `sidecar_path:`/`provision_key:` — write freely there; files older than 24h are reaped.**
<!-- END VARIANT -->

<!-- VARIANT:assessment -->
**Your provisioned home for this dispatch: `state/subagent-share/<session-id>/<provision_key>.md` — git-tracked, assessment-typed (question/answer shape), created for your role before you start. Record your findings and answer there as you go, then return only a terse pointer — `done: <path>`, never a full dump. Your final message spends the EM's context window; the sidecar doesn't. Fall back to `scratch/subagent-sandbox/` (root-level, off `state/`) only if your dispatch carries no `sidecar_path:`/`provision_key:` — write freely there; files older than 24h are reaped.**
<!-- END VARIANT -->
