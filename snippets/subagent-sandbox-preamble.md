<!-- canonical source for subagent-sandbox-preamble — edit here, then run verify-subagent-sandbox-preamble-sync.py --fix -->
<!-- consumers: discovered at runtime by bin/verify-subagent-sandbox-preamble-sync.py via its _CONSUMER_ROLE_MAP (scoped Sonnet/Haiku single-job workers — scouts, specialists, workers, checkers, auditors). Each consumer's report_type_map: template type (subagent-sandbox-policy.yaml) selects exactly one VARIANT block below, naming that role's own provisioned typed doc. Exclusions, each for a different reason: (1) the six Opus reviewer personas get the `contract_blocks:`-injected sidecar-frontmatter-contract + persona-persisting-findings family; (2) enricher/executor/review-integrator/test-runner get `run-report-citizenship` via `contract_blocks:` — their `run-report` shape (status/agent_type/divergence/commits) matches neither VARIANT; (3) docs-checker, a G2 pre-flight emitter, gets only sidecar-emission-contract via `contract_blocks:` (prior-art-checker/plan-coverage-checker/external-pattern-checker carry both it and this preamble). Named-dispatch relay: `VARIANT:review-findings` carries a resident copy (all consumers class 2); `VARIANT:assessment` consumers span classes 1 and 2 and instead receive one of `named-dispatch-relay`, `named-dispatch-relay-named-scout`, `named-dispatch-class-3-backstop` via `contract_blocks:`, wired per-consumer in subagent-sandbox-policy.yaml. Keep this list consistent with that policy file. -->

<!-- VARIANT:review-findings -->
**Provisioned home: `state/subagent-share/<session-id>/<provision_key>.md` — git-tracked, review-findings-typed (one disposition slot per finding), created for your role before you start. Record each finding's disposition there as you go; return only a terse pointer, `done: <path>`, never a full dump. No `sidecar_path:`/`provision_key:` in your dispatch → fall back to `scratch/subagent-sandbox/` (root-level, off `state/`); files there are reaped after 24h.**
**Named dispatch?** A teammate's return text never arrives — `SendMessage` this pointer to `"main"`.
<!-- END VARIANT -->

<!-- VARIANT:assessment -->
**Provisioned home: `state/subagent-share/<session-id>/<provision_key>.md` — git-tracked, assessment-typed (question/answer shape), created for your role before you start. Record your findings and answer there as you go; return only a terse pointer, `done: <path>`, never a full dump. No `sidecar_path:`/`provision_key:` in your dispatch → fall back to `scratch/subagent-sandbox/` (root-level, off `state/`); files there are reaped after 24h.**
<!-- END VARIANT -->
