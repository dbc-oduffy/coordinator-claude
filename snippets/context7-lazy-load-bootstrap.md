<!-- canonical source for context7-lazy-load-bootstrap — edit here, then run bin/verify-snippet-sync context7-lazy-load-bootstrap --fix -->
<!-- consumers: fixed list in snippets/registry.toml [snippet.context7-lazy-load-bootstrap] -->
<!-- INJECTED block, not paste-governed: assembled into the dispatched child prompt at dispatch -->
<!-- time via the `contract_blocks:` grammar (subagent-sandbox-policy.yaml), keyed by -->
<!-- `subagent_type`. Carries the static Context7 lazy-load bootstrap mechanics — the -->
<!-- `ToolSearch("select:...")` incantation and the snake_case-fallback caveat — zero -->
<!-- per-consumer customization. Relocated from 3 byte-identical resident carriers -->
<!-- (senior-front-end, staff-data-sci, staff-ux) per -->
<!-- state/audits/2026-08-06-cross-body-duplication-census.md § Span 3. Also wired to staff-eng, -->
<!-- eng-director, and executor: those three carried independently-worded paraphrases of the same -->
<!-- fact ("try the underscore variant" / "snake_case fallback if empty" / "retry with -->
<!-- underscores") — leaving three divergent hand-written paraphrases beside one canonical -->
<!-- injected block is exactly the drift injection exists to prevent, so all six converge on the -->
<!-- canonical wording (EM decision at wiring time, census left this an open call). -->
<!-- PLACEHOLDER-FREE BY CONSTRUCTION: do not add any placeholder here. The closed set is -->
<!-- {kind, sidecar_path, subagent_type} and this block needs none of them; the closed placeholder -->
<!-- set is enforced by coordinator/tests/test_contract_blocks.py. -->

**Lazy-loaded** — bootstrap: `ToolSearch("select:mcp__plugin_context7_context7__resolve-library-id,mcp__plugin_context7_context7__query-docs")` (snake_case fallback if empty).
