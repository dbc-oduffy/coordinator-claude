<!-- canonical source for named-dispatch-relay-named-scout — edit here, then run bin/verify-snippet-sync named-dispatch-relay-named-scout --fix -->
<!-- consumers: fixed list in snippets/registry.toml [snippet.named-dispatch-relay-named-scout] -->
<!-- INJECTED block, not paste-governed: assembled into the dispatched child prompt at dispatch -->
<!-- time via the `contract_blocks:` grammar (subagent-sandbox-policy.yaml), keyed by -->
<!-- `subagent_type`. Extracted from `subagent-sandbox-preamble.md`'s `VARIANT:assessment` body -->
<!-- (C4, named-dispatch three-class partition) as a rewritten variant for exactly the three -->
<!-- named scouts (research-scout, repo-scout, notebooklm-research-scout) — per the namer census -->
<!-- these are now class 1, named on purpose and addressed by `owner:`, never by message, and none -->
<!-- of the three declares `SendMessage`. The standard `SendMessage`-to-`"main"` prescription this -->
<!-- block replaces would be actively wrong advice for them. Not pasted by verify-snippet-sync. -->
<!-- named-dispatch-clause: relay — machine-read by test_reporting_type_set_matches_the_agent_definitions to decide whether a type this block is injected into counts as clause-carrying. `relay` names how the report reaches the dispatcher and IS the clause; `backstop` warns without prescribing a route and is NOT, so class 3 stays disjoint from the clause-carrying set. -->

**Named dispatch?** You are named on purpose and addressed by `owner:`, never by message — signal
completion via `TaskUpdate`, not `SendMessage`; you hold no such tool.
