# Expected routing manifest for sample-lessons/ (per-entry YAML directory)
# Validates the four-record routing produced by /lesson-triage cross-project mode
# against the canonical change-kind taxonomy + per-lesson schema.
# Migrated from sample-lessons.md (markdown) to per-entry YAML format 2026-06-30.
# Sources reference the fixture YAML files in sample-lessons/ (1-based sorted-file-index).

- id: "fixture-L1"
  source: "tests/fixtures/lesson-triage/sample-lessons/2026-01-01-subagent-dispatch-billing-inheritance.yaml:1"
  summary: "Subagent dispatch billing inheritance"
  scope: universal
  destinations:
    - target: "plugins/coordinator-claude/coordinator/CLAUDE.md"
      section: "(new section) Subagent Dispatch — Billing Tier Inheritance"
      change_kind: doctrine-edit
      rationale: "applies to every coord plugin user, not just one repo"
      priority: HIGH
    - target: "tests/fixtures/lesson-triage/sample-lessons/2026-01-01-subagent-dispatch-billing-inheritance.yaml:1"
      section: "(this entry)"
      change_kind: strip-local
      rationale: "promoted to central; gate on doctrine-edit commit SHA"
      priority: HIGH
      depends_on: "fixture-L1.destinations[0]"
  open_questions: []

- id: "fixture-L2"
  source: "tests/fixtures/lesson-triage/sample-lessons/2026-01-02-skeletal-mesh-socket-attachment-ordering.yaml:2"
  summary: "Skeletal mesh socket attachment ordering"
  scope: project
  destinations:
    - target: "tests/fixtures/lesson-triage/sample-lessons/2026-01-02-skeletal-mesh-socket-attachment-ordering.yaml:2"
      section: "(this entry)"
      change_kind: retag-local
      rationale: "already correctly tagged as project-scoped; no action needed beyond confirming"
      priority: LOW
  open_questions: []

- id: "fixture-L3"
  source: "tests/fixtures/lesson-triage/sample-lessons/2026-01-03-build-script-auto-discovery-sweeps.yaml:3"
  summary: "Build script auto-discovery sweeps stale backups"
  scope: project
  destinations:
    - target: "<originating-project>/build.sh"
      section: "auto-discovery glob block"
      change_kind: project-structural
      rationale: "concrete code change in the originating repo, not central doctrine"
      priority: MEDIUM
    - target: "docs/wiki/cleanup-sweep-hazards.md"
      section: "(new file)"
      change_kind: wiki-new
      rationale: "battle-story preservation — pattern recurs across projects"
      priority: MEDIUM
  open_questions:
    - "which originating project? source-repo context is in from_repo field if set"

- id: "fixture-L4"
  source: "tests/fixtures/lesson-triage/sample-lessons/2026-01-04-text-only-hallucination-disk-first-verification.yaml:4"
  summary: "TEXT-ONLY hallucination — disk-first verification"
  scope: universal
  destinations:
    - target: "tests/fixtures/lesson-triage/sample-lessons/2026-01-04-text-only-hallucination-disk-first-verification.yaml:4"
      section: "(this entry)"
      change_kind: strip-local
      rationale: "already encoded centrally per the entry's own admission"
      priority: HIGH
  open_questions: []
