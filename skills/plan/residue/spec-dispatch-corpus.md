---
segment_id: spec-dispatch-corpus
route: spec-dispatch
class: protected
order: 890
---

- **None, but `scope_mode` is `production-patch`/`spec-dispatch`/audit** → a single restating sentence, not the full triad. Proportional to scope, and honours P5: no gate fires heavily on trivial work. **Its un-gaming clause is proportional too — falsifiability, not the three below:** state the sentence so a PM reading it could answer *"no, that is not the problem."* One that cannot be answered "no" is the banned self-report wearing a sentence's clothes.

- _Composing the S-lane body?_ → `route: spec-dispatch` skips Branch C entirely, so an S-lane plan is born prime-exit-criterion-less unless named here. The four-part body (problem sentence, file scope, acceptance criteria, test surface) becomes five: add the prime exit criterion statement plus its `derived_from` link, one line. **No baseline falsifier** — § Proportionality exempts this lane by size, and writing the falsifier into the S-lane corpus would re-impose the cost the PM ruled out. A cheaper lane is not an exemption from stating what the work is for; it is an exemption from the apparatus for proving it.

| `spec-dispatch` | **Light terminal.** After the write-time commit: run the cross-plan conflict scan against the S-lane body, then dispatch an executor directly. No Opus plan review — the honest trade is to implement and let `code-reviewer` catch the diff. The lane owes a scoped `code-reviewer` pass over the executor's diff, integrated via `review-integrator`, before any closing ceremony — regardless of which of the lane's exits the session takes: the four canonical session terminators (`quick-wrap`, `/workstream-complete`, `/handoff`, `/workday-complete`) and the merge path (`/merging-to-main`) alike. **Never skips scoped-commit discipline or ask-before-external-action.** |
